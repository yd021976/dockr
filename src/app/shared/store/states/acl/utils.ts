import * as roles from './entities-management/roles'
import * as services from './entities-management/services'
import * as actions from './entities-management/actions'
import * as fields from './entities-management/fields'
import * as treenodes from './entities-management/treenodes'

export const entity_management = {
    roles: roles,
    services: services,
    actions: actions,
    fields: fields,
    treenodes: treenodes
}